import type { Metadata } from "next";
import { CheckCircle2, FileCheck2, SearchCheck } from "lucide-react";
import { PropertySubmissionForm } from "@/components/forms/PropertySubmissionForm";
import { SectionHeading } from "@/components/sections/SectionHeading";
import { Card } from "@/components/ui/card";
import { createMetadata } from "@/lib/seo";
export const metadata: Metadata = createMetadata({ title: "Sell Your Property", description: "Submit your plot, apartment, villa, land, house, or commercial property to BhoomiKonnect for admin review and verified buyer enquiries.", path: "/sell-property" });
export default function SellPropertyPage() { return <><section className="border-b bg-muted/50 py-10 sm:py-14"><div className="container"><SectionHeading eyebrow="Property owners" title="Sell or rent with a reviewed listing." description="Share the essentials now. BhoomiKonnect checks ownership, approvals, pricing context, and media before a property can be published." /></div></section><section className="py-10 sm:py-14"><div className="container grid gap-8 lg:grid-cols-[0.72fr_1.28fr]"><div className="space-y-4">{[[FileCheck2, "Submit details", "Tell us the property type, location, size, price, and document status."], [SearchCheck, "Admin review", "The team validates the information and requests any missing ownership or approval evidence."], [CheckCircle2, "Publish when approved", "Only reviewed listings move into the marketplace and buyer lead workflow."]].map(([Icon, title, copy], index) => <Card key={String(title)} className="p-5"><span className="text-xs font-bold text-primary">0{index + 1}</span><Icon className="mt-4 size-6 text-primary" aria-hidden /><h2 className="mt-3 text-lg font-bold">{String(title)}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{String(copy)}</p></Card>)}</div><PropertySubmissionForm /></div></section></>; }
